<main class="container">
  <%= link_to sanitize("&larr; Back to visits"), visits_path %>

  <div class="header">
    <h1>Assign visit request</h1>
  </div>

  <%= turbo_frame_tag dom_id(@visit, "index") do %>
    <%= render "assign-form", visit: @visit %>
  <% end %>
</main>
